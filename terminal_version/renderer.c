#include "renderer.h"

/* ============================ MAIN DISPLAY ==========================================*/

int showDisplay(Display* display, Cursor* cursor )
{
    int sizeX = sizeof(display);
    int sizeY = sizeof(display[0]);
    enum elementType element = cursor->typeSelected;

    

}